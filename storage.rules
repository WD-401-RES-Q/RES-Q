rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read ID photos (for admin verification)
    match /id_photos/{allPaths=**} {
      allow read: if true;
      // Allow unauthenticated writes for registration flow
      allow write: if true;
    }

    // Report media uploaded from the mobile app.
    // Keep uploads working with the current custom login flow even when
    // Firebase anonymous auth is disabled.
    match /reports/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }

    // Deny everything else by default
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
